apiVersion: v1
kind: Service
metadata:
  name: {{ k8s_nginx_service }}
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: {{ k8s_nginx_app }}
  type: {{ k8s_nginx_type }}

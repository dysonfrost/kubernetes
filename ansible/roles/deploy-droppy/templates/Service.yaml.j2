apiVersion: v1
kind: Service
metadata:
  namespace: {{ k8s_droppy_namespace }}
  name: {{ k8s_droppy_service }}
spec:
  type: {{ k8s_droppy_service_type }}
  ports:
  - name: http
    port: 80
    targetPort: 8989
  selector:
    app: {{ k8s_droppy_app }}
